# Mikser

Per-app volume mixer for macOS, menu bar only. Personal project. See BRIEF.md for the
decided design, plan.json for the build tasks, GATES.md for the moments that need a human.

Build: `swift build -c release` · Tests: `swift run mikser-selftest` · App bundle: `scripts/build.sh`
